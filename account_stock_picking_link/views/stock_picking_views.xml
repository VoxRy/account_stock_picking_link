<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form_inherit_invoice_link" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.invoice.link</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_invoice"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-pencil-square-o"
                        attrs="{'invisible': [('invoice_count', '=', 0)]}">
                    <field name="invoice_count" widget="statinfo" string="Faturalar"/>
                </button>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Faturalar" name="invoice_page">
                    <field name="invoice_ids" widget="many2many_tags" 
                           options="{'no_create': True}"/>
                </page>
            </xpath>
        </field>
    </record>
</odoo>

